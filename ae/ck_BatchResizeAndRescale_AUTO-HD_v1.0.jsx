function init(){    upres_comps(1920, 1080);}function upres_comps(targWidth, targHeight){    app.beginUndoGroup("Resize and Rescale (AUTO HD)");   var projCollection = app.project.selection;   // all selected items      for(a=0; a<projCollection.length; a++){      if(projCollection[a].layers){		 if (projCollection[a] instanceof CompItem) {			 			 projCollection[a].width = targWidth;			 projCollection[a].height = targHeight;			 			 myLayers = projCollection[a].layers;			 			for(k=1;k<=myLayers.length;k++){                       var targX = (targWidth / myLayers[k].width) * 100;                       var targY = (targHeight / myLayers[k].height) * 100;                       var targScale = [targX,targY];					myLayers[k].scale.setValue(targScale);					myLayers[k].position.setValue([targWidth/2,targHeight/2]);			  }		  		   }      }   }      app.endUndoGroup();}init();		