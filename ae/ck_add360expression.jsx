function add360expression(){	app.beginUndoGroup("Add 360 Expression");	var myExpression = "maxFrame = comp(this.name).duration;minAngle = 0;maxAngle = 360;curAngle = effect(\"Angle Control\")(\"Angle\") % maxAngle;if(curAngle < minAngle){curAngle = maxAngle - (curAngle * -1);}framesPerDegree = maxFrame / maxAngle;calculatedFrame = curAngle * framesPerDegree;calculatedFrame;";  	var activeComp = app.project.activeItem;  	var layerColl  = activeComp.selectedLayers; // all selected layers   		for(a=0; a<layerColl.length; a++){		var thisLayer = layerColl[a];						thisLayer.property("Effects").addProperty("ADBE Angle Control");		thisLayer.timeRemapEnabled = true;		thisLayer.property("Time Remap").expression = myExpression;	}   		app.endUndoGroup();}add360expression();		