function init(){    extend_layers();}function extend_layers(targWidth, targHeight){    app.beginUndoGroup("Extend Layers To Comp");   var projCollection = app.project.selection;   // all selected comp items      for(a=0; a<projCollection.length; a++){      if(projCollection[a].layers){		 if (projCollection[a] instanceof CompItem) {			 			 //get comp duration			 var targLength = projCollection[a].duration;			 			 var myLayers = projCollection[a].layers;			 				for(k=1;k<=myLayers.length;k++){					myLayers[k].outPoint = targLength;				}		  		   }      }   }      app.endUndoGroup();}init();		