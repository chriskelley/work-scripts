function init(){	var search = prompt("Old String:","");	var replace = prompt("New String:","");		if(search != null && replace != null){		NameReplace(search, replace);	}}function NameReplace(search, replace){	 app.beginUndoGroup("Name Replace");      var projCollection = app.project.selection;   // all selected items      for(a=0; a<projCollection.length; a++){      if(projCollection[a].layers){		 if (projCollection[a] instanceof CompItem) {				origname = projCollection[a].name;				projCollection[a].name = origname.replace(search, replace);		   }      }   }      app.endUndoGroup();}init();		