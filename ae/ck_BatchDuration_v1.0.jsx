function init(){	var dur = prompt("What is your target duration (frames)","");		if(dur != null){		changeDuration(dur);	}}function changeDuration(dur){	 app.beginUndoGroup("changeCompDuration");      var projCollection = app.project.selection;   // all items      for(a=0; a<projCollection.length; a++){      if(projCollection[a].layers){		 if (projCollection[a] instanceof CompItem) {				projCollection[a].duration = parseInt(dur) / projCollection[a].frameRate;		   }      }   }      app.endUndoGroup();}init();		