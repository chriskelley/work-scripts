function init(){	var newWidth = prompt("What is your target width:","");	var newHeight = prompt("What is your target height:","");	if(newWidth != null && newHeight != null){		changeSize(newWidth, newHeight);	}}function changeSize(newWidth, newHeight){	 app.beginUndoGroup("BatchResizeComps");      var projCollection = app.project.selection;   // all items      for(a=0; a<projCollection.length; a++){      if(projCollection[a].layers){		 if (projCollection[a] instanceof CompItem) {				projCollection[a].height = parseInt(newHeight);				projCollection[a].width = parseInt(newWidth);		   }      }   }      app.endUndoGroup();}init();		